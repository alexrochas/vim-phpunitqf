language: python
python:
  - "2.7"
  - "3.3"
before_install:
  - pip install codecov
# command to install dependencies
install: "pip install -r requirements.txt"
# command to run tests
script: nosetests --with-coverage --cover-package plugin
after_success:
  - codecov
